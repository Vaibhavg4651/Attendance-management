 <button className='btn btn-primary' onClick={handlefilter}>Apply filters</button>
          <span className='mx-2'></span>
        <ReactToExcel
      table="table-to-xls"
      filename="excelFile"
      sheet="sheet 1"
      className="btn btn-success"
      onClick={exportToExcel}
      />

       const exportToExcel = () => {
    const data = students.map((student) => ({
      'S.No.': student[''],
      Name: student['__1'],
      'Enrollment no.': student['__8'],
      Status: attendance[student['']] || 'absent', // default to absent if not marked
    }));
    const ws = XLSX.utils.json_to_sheet(data);
    const wb = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(wb, ws, 'Attendance Sheet');
    XLSX.writeFile(wb, 'Attendance.xlsx');
  }


   <div>
        <div className="Container2">
          <div className="row justify-content-center mb-4">
            <div className="col md-5">
              <div className="card2 mt-5 login-custom ">
                <div className="card-body">
                  {/* <input
                    className="mt-4"
                    type="number"
                    placeholder="Enter the subjectid"
                    onChange={(e) => setSubjectId(e.target.value)}
                  /> */}
                    <input
                      className="mt-4"
                      type="text"
                      placeholder="Enter branch name"
                      onChange={(e) => setBranchName(e.target.value)}
                    />
                  <input
                    className="mt-4"
                    type="text"
                    placeholder="Enter Subject name"
                    onChange={(e) => setSubjectName(e.target.value)}
                  />
                  <input
                    className="mt-4"
                    type="text"
                    placeholder="Enter Subject Code"
                    onChange={(e) => setSubjectCode(e.target.value)}
                  />
                  <input
                    className="mt-4"
                    type="text"
                    placeholder="Enter Subject Type"
                    onChange={(e) => setSubjectType(e.target.value)}
                  />
                  <input
                    className="mt-4"
                    type="number"
                    placeholder="Enter the Year"
                    onChange={(e) => setYear(e.target.value)}
                  />

                  <button
                    className="btn btn-primary"
                    onClick={handleaddSubjects}
                  >
                    submit
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>


       // <div className='filter-modal'>
        //   <label>Date:</label>
        //   <input type="date" placeholder="Enter date" onChange={(e) => handleStartDateChange(e.target.value)} />
        //   <br />
        //   <br />
        //   <div className='filter-item'>
        //     {/* <h4 className='text-center'>OR</h4> */}
        //     <label>Start Date:</label>
        //     <input type="date" onChange={(e) => handleStartDateChange(e.target.value)} />
        //   </div>
        //   <br />
        //   <div>
        //     <label>End Date:</label>
        //     <input type="date" onChange={(e) => handleEndDateChange(e.target.value)} />
        //   </div>
        //   <br />
        //     {/* <h4 className='text-center'>OR</h4> */}
        //   <div>
        //   <select
        //             className=""
        //             value={Percentage}
        //             onChange={(e) => setPercentage(e.target.value)}
        //           >
        //             <option value="select role">Select by Percentage</option>
        //             <option value="proctor">Below 30</option>
        //             <option value="proctor">30-40%</option>
        //             <option value="proctor">50-60%</option>
        //             <option value="proctor">60-70%</option>
        //             <option value="faculty">70-80%</option>
        //           </select>
        //   </div>
        //     <button className="btn btn-primary" onClick={applyFilters}>
        //     Apply
        //   </button>
        // </div>